SELECT id FROM transactions 
WHERE status = 'delivered' 
  AND delivered_at < NOW() - INTERVAL '24 hours' 
  AND buyer_confirmed_at IS NULL;

#"24 saat içinde doğrulamazsa otomatik tamamlansın" kuralın yazılım dünyasında asenkron bir zamanlanmış görev (Cron Job veya Background Worker) gerektirir.

FastAPI ayağa kalktığında, arka planda (örneğin saatte bir veya her 15 dakikada bir) çalışacak bir script yazacağız. Bu script SQL'de şu sorguyu çalıştıracak:

Dönen sonuçlardaki tüm işlemlerin durumunu otomatik olarak completed çekeceğiz. Böylece sistem kendi kendini yöneten bir otopilota dönüşecek.